id: '@stitchmate/frontend/user.config'

project:
  description: 'Frontend package that maintains our User configuration client-side.'
  maintainers: ['@cawfeecoder']

language: 'typescript'
type: 'library'

fileGroups:
  sources:
    - 'src/**/*'
  outputs:
      - 'dist'

tasks:
  build:
    command: 'vite build'
    inputs:
      - '@globs(sources)'

tags:
  - frontend